{
  "name": "zettnote",
  "displayName": "zettnote",
  "description": "Extension to support Zettelkasten markdown notes",
  "version": "0.0.1",
  "engines": {
    "vscode": "^1.45.0"
  },
  "categories": [
    "Other"
  ],
  "activationEvents": [
    "onLanguage:markdown"
  ],
  "main": "./out/extension.js",
  "contributes": {
    "viewsContainers": {
      "activitybar": [
        {
          "id": "zettnote-explorer",
          "title": "ZettNote Explorer",
          "icon": "resources/explorer.svg"
        }
      ]
    },
    "views": {
      "zettnote-explorer": [
        {
          "id": "znotes",
          "name": "NOTES"
        },
        {
          "id": "zlinks",
          "name": "LINKS"
        },
        {
          "id": "zbacklinks",
          "name": "BACKLINKS"
        }
      ]
    },
    "commands": [
      {
        "command": "zettnote.updateTreeViews",
        "title": "ZettNote Update Links"
      },
      {
        "command": "zettnote.getLinks",
        "title": "ZettNote Get Links"
      },
      {
        "command": "zettnote.getBacklinks",
        "title": "ZettNote Get Backlinks"
      }
    ]
  },
  "scripts": {
    "vscode:prepublish": "npm run compile",
    "compile": "tsc -p ./",
    "lint": "eslint src --ext ts",
    "watch": "tsc -watch -p ./",
    "pretest": "npm run compile && npm run lint",
    "test": "node ./out/test/runTest.js"
  },
  "devDependencies": {
    "@types/glob": "^7.1.1",
    "@types/mocha": "^7.0.2",
    "@types/node": "^13.11.0",
    "@types/vscode": "^1.45.0",
    "@typescript-eslint/eslint-plugin": "^2.30.0",
    "@typescript-eslint/parser": "^2.30.0",
    "eslint": "^6.8.0",
    "glob": "^7.1.6",
    "graph-data-structure": "^1.12.0",
    "mocha": "^7.1.2",
    "typescript": "^3.9.2",
    "vscode-test": "^1.3.0"
  }
}
